rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Modo Teste: Permite leitura/escrita global por tempo limitado para a fase de testes
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 4, 16);
    }
  }
}
